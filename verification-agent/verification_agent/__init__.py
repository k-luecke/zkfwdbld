"""Verification-Agent — autonomous, human-supervised vulnerability finder.

Specialized for verification / reachability / cross-chain / ZK scopes. Core
discipline: verify-or-discard — no finding leaves the system without a
fork-executed, reproducing PoC.

This package currently implements **M0 only** (the harness). See STATUS.md for
the honest, per-module build state.
"""

__version__ = "0.0.1"
