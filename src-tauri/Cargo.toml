[package]
name = "zkfwdbld-desktop"
version = "0.1.0"
description = "Desktop shell for the zkfwdbld trust layer viewer"
authors = ["Kyle W. Luecke"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

