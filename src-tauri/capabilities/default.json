{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the zkfwdbld desktop window.",
  "windows": ["main"],
  "permissions": ["core:window:default", "core:webview:default"]
}
